var greet = require('./greet');
// require() passing parameter is a string of the location of the module we want to bring 
// require by itself will execute it 
// a variable will store whatever we have attached to module.exports()
greet();