module.exports.greet = function() {
	console.log('Hello world!');
};
// instead of replacing the empty object that is already there on exports property of module object, we can add a new property to object
